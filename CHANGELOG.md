# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 结构，并使用语义化版本号。

## [Unreleased]

## [1.1.2] - 2026-07-22

### 升级说明

- `v1.1.0` 及更早版本的旧更新器无法用远端代码修复自身；请从 GitHub Releases 手动运行一次 `v1.1.2` 安装包，之后的版本可继续使用应用内更新。

### 修复

- 更新安装改用 `electron-updater` 的标准可见 NSIS 流程，恢复安装器提权、启动失败兜底和原安装目录复用，不再经过隐藏命令行或静默安装路径。
- 安装前强制完成 WAL checkpoint，创建独立的更新前数据库备份，并验证 SQLite 完整性与账号、2FA、标签、自定义字段和服务信息记录数。
- 在更新退出事件中关闭数据库；记录并恢复失败的安装尝试，校验下载文件存在后才允许安装，并防止检查、下载与安装操作重叠。
- 修复设置页首次读取更新状态失败后无法重试的问题，并防止过期 IPC 回包覆盖较新的下载或错误状态。

### 工程

- 增加轮转更新日志、更新状态机与持久化测试，并校验打包产物中的 `app-update.yml` 和 `elevate.exe`。
- 设置页始终提供 GitHub Releases 与更新日志入口，便于自动安装失败时手动恢复。

## [1.1.1] - 2026-07-22

### 新增

- 账号标签支持从当前账号移除，已创建标签列表支持滚动查看全部内容。
- 首次为账号添加 2FA 时可确认是否同步创建独立卡片，并保留 HOTP、算法、位数、周期与计数器参数。

### 修复

- 修复更新账号 2FA 密钥时独立卡片未同步或重复创建的问题；账号与关联 2FA 现在使用同一事务写入，失败时整体回滚。
- 清空账号 2FA 密钥时保留原记录并解除关联；检测到历史重复关联时停止覆盖并提示人工确认。
- 修复标签及自定义字段增删后可能覆盖尚未保存的账号密码、2FA 或备注草稿的问题。
- 修复标签较多时建议项无法访问、筛选后拖动账号会破坏隐藏账号顺序的问题。
- 修复 HOTP 计数器递增、2FA 参数更新、账号恢复或备份导入后部分界面未及时刷新或显示旧验证码的问题。
- 修复回收站和 2FA 删除失败仍显示成功，以及已回收或已彻底删除账号的 2FA 状态不明确的问题。
- 防止 HOTP 计数器快速连点导致多次递增，并拒绝递增不存在或非 HOTP 的记录。
- 区分“数据库写入失败”和“写入成功但界面刷新失败”，避免账号、2FA、标签、字段、服务或分组被误导重试后重复创建。
- 账号、2FA、回收站与服务信息读取失败时显示错误和重试入口，不再把读取失败展示为“暂无数据”。
- 服务、API 密钥字段及分组的创建、编辑、删除、移动和排序增加防重复提交、过期结果校验与部分成功反馈。
- 账号彻底删除增加事务回滚保护；仅删除回收站中的账号，并将关联 2FA 保留为孤立记录。
- 导入前必须完成 WAL checkpoint 并验证安全备份；旧版 JSON 恢复会保留仍然有效的服务账号关联。
- 修复应用内“重启并安装”没有启动已下载更新的问题，并补充安装中与失败恢复状态。
- 更新 `fast-uri` 安全补丁，修复依赖审计报告的高危漏洞。

## [1.1.0] - 2026-07-14

### 新增

- 账号自定义字段支持填写、编辑、敏感值遮罩和删除确认。
- 2FA 支持编辑，并完整保留 URI 中的算法、位数、周期和 HOTP 计数器。
- 回收站支持恢复或永久删除服务信息。
- 侧栏统一使用“回收站”命名，并明确同时包含账号和服务。
- 服务信息支持 URL、备注、关联主账号、详情收藏和真实拖拽排序。
- 数据库导入前增加 JSON 结构与 SQLite 完整性验证。
- 导入、导出和 CSV 操作增加结果反馈与错误提示。
- CSV 导入支持 OTP URI 参数、关联 2FA 卡片、手机号和恢复邮箱，并报告无效或不可识别行。
- 增加离线 CSP、渲染沙箱和外部导航限制，移除启动时的第三方字体请求。

### 修复

- 修复加密用户名无法被账号搜索命中的问题。
- 修复敏感账号自定义字段明文展示及更新时可能降级为明文的问题。
- 修复账号密码、2FA 密钥和服务敏感字段进入编辑状态后默认明文展示的问题。
- 修复 TOTP 密钥在数据库和 JSON 备份中以明文保存的问题。
- 修复 JSON 恢复丢失 2FA 与账号关联的问题。
- 修复导入 SQLite 后服务信息仍使用旧数据库连接的问题。
- 修复 WAL 模式下直接复制数据库可能遗漏最新记录的问题。
- 修复取消账号编辑后未保存内容再次出现的问题。
- 修复编辑账号时切换账号或新建账号会静默丢失未保存内容的问题。
- 修复编辑自定义字段时切换账号或页面会静默丢失未保存内容的问题。
- 未实际修改内容时不再弹出“放弃未保存修改”确认，并禁用无变化的保存操作。
- 关闭应用窗口时若仍有未保存账号或自定义字段修改，会先请求确认。
- 修复服务使用名称、更新时间等自动排序时仍可拖动但顺序看似不生效的问题。
- 服务、字段和分组删除统一使用应用内确认框，网址打开失败改用非阻塞消息提示。
- 修复“其他”类型账号被默认命名为 Microsoft 账号的问题。
- 修复账号与关联 2FA 同时更新时名称不同步、删除 2FA 后主账号仍残留密钥的问题。
- 修复关联 2FA 使用非默认算法、位数、周期或 HOTP 时，主账号详情生成错误验证码的问题。
- 修复明文旧备份导入后敏感账号与服务字段未重新加密的问题。
- 修复导入数据库失败后连接不可用，并防止同秒自动备份相互覆盖。
- 修复使用大写 `.DB` 扩展名时 SQLite 备份被误按 JSON 处理的问题。
- 修复账号搜索或平台筛选影响服务关联下拉框、2FA 分组及关联账号跳转的问题。
- 增加单实例锁，避免同一用户数据目录被多个应用进程并发打开。
- 限制账号和服务永久删除只能作用于回收站记录。
- 密码生成器改用系统加密安全随机数。

### 工程

- 统一界面偏好到主进程存储，并迁移旧的 localStorage 设置。
- 清理历史构建日志，完善忽略规则、测试与发布校验。
- 升级 Electron、Vite、electron-builder、better-sqlite3 等运行与打包依赖，移除已知高危依赖链。
- CI 使用 Node.js 22 LTS（最低 22.12），审计完整依赖树，并增加 Pull Request 核对模板。
- Release Action 在上传前校验安装包、blockmap 与 `latest.yml` 的版本、路径、大小和 SHA-512。
- GitHub Actions 升级到 Node.js 24 Runtime 对应主版本，移除 Node.js 20 弃用警告。
- README 增加基于纯演示数据生成的账号、服务、2FA 与统一回收站界面预览。

## [1.0.3] - 2026-07-07

### 修复

- 稳定 Windows 自动更新下载、退出和静默安装流程。
- 更新安装前主动 checkpoint 并关闭 SQLite 数据库。
- 修正服务信息与部分界面的更新安装兼容问题。

## [1.0.2] - 2026-07-07

### 工程

- 完善 CredVaultix 产品身份、安装包名称和 GitHub 发布配置。
- 增加测试、类型检查和 Windows Release Action。

## [1.0.1] - 2026-07-07

### 新增

- 增加服务信息库、字段分组、收藏、搜索与导入导出。
- 增加侧栏偏好和数据目录入口。

## [1.0.0] - 2026-03-07

### 新增

- 发布本地账号、密码、2FA、标签和回收站管理基础版本。
